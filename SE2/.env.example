# Optional: set to use OpenAI for extraction (otherwise regex fallback is used).
# Do not commit real keys. Use .env (gitignored) for local development.
OPENAI_API_KEY=sk-your-openai-api-key-here

# Required for real-time voice (voice_server.py). Same protocol as dashboard/index.html.
DEEPGRAM_API_KEY=your-deepgram-api-key
# Optional: voice server port (default 8080), SE2 API URL (default http://localhost:8000)
# VOICE_PORT=8080
# SE2_API_URL=http://localhost:8000

# Clustering: reduce merging of different incidents (higher = fewer merges; semantic guards = require embedding+LLM agreement)
# CLUSTER_THRESHOLD=0.65
# CLUSTER_WEIGHTS=0.35,0.35,0.15,0.15
# CLUSTER_MIN_EMBEDDING=0.4
# CLUSTER_MIN_LLM=0.45

# Snowflake analytics (optional): incident snapshots, timeline events, chunk events
# SNOWFLAKE_ACCOUNT=your_account
# SNOWFLAKE_USER=your_user
# SNOWFLAKE_PASSWORD=your_password
# SNOWFLAKE_WAREHOUSE=your_warehouse
# SNOWFLAKE_DATABASE=your_database
# SNOWFLAKE_SCHEMA=PUBLIC
# SNOWFLAKE_INCIDENTS_TABLE=incident_snapshots
# SNOWFLAKE_TIMELINE_TABLE=timeline_events
# SNOWFLAKE_CHUNKS_TABLE=chunk_events
